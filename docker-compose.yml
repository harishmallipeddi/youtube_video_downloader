version: '3.8'

services:
  youtube-downloader:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: youtube_downloader_app
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PORT=8000
      - HOST=0.0.0.0
